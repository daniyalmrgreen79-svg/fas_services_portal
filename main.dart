import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    home: Scaffold(
      appBar: AppBar(title: Text("FAS Services Portal")),
      body: Center(child: Text("Welcome to FAS Services Portal!")),
    ),
  ));
}
